{
  "name": "portfolio-2026",
  "version": "1.0.0",
  "description": "Karl Goldstraw's personal website and blog, built with Eleventy",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site",
    "test": "npm run build && node test/run.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-navigation": "^1.0.5",
    "@11ty/eleventy-plugin-rss": "^3.0.0",
    "axe-core": "^4.13.0",
    "playwright": "^1.63.0"
  }
}
